# Osiris (local CCTV work)

Local checkout of Osiris lives in `osint/`.

`origin` is still `simplifaisoul/osiris` — **do not push env or this handoff commit there**.
This copy is `jauzza/osiris` (private) and includes your Spain/Netherlands CCTV work + env if present.

## Run
See README. `npm install && npm run dev`.

## Chat notes

See `handoff/chats/cursor-notes.md` for recent Cursor user messages. The Cursor sidebar will not restore on another PC.
